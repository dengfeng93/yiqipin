name: yiqipin
description: 一起拼 — 基于位置的组局拼单App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  riverpod: ^2.5.1
  dio: ^5.4.3+1
  flutter_secure_storage: ^9.2.2
  socket_io_client: ^2.0.3+1
  amap_flutter_location: ^3.0.0
  cached_network_image: ^3.3.1
  image_picker: ^1.1.1
  share_plus: ^9.0.0
  path_provider: ^2.1.3
  webview_flutter: ^4.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
